---
"@pie-players/pie-section-player": patch
---

Default section-player passage and question card title color to the themed `--pie-text` variable (with `--pie-header-text` still an opt-in host override) so card titles stay legible across DaisyUI dark and high-contrast themes instead of being stuck at a hardcoded near-black.
