# Accessibility Review - 2026-03-20

This document records the project-wide accessibility review execution for the current pass.

## Scope

- Tier 1: core player and tool custom-element surfaces.
- Tier 2: integration/demo wrappers that host player flows.
- Tier 3: support/debug panels visible in demo flows.

Scope details are aligned with `project-surface-map.md` and `coverage-matrix.md`.

## Automated Evidence Run

Executed from repo root:

```bash
bun run test:e2e:a11y:critical
```

Current critical suite includes:

- `section-player` critical route with axe baseline.
- `item-player` multiple-choice flow with scoped axe baseline.
- `assessment-player` smoke flow including keyboard/focus assertions and scoped axe baseline.

Result: passed in this review pass.

## Findings

### High / Blocker

- No new unexpected serious/critical axe violations were introduced by this pass in the critical suite.

### Known critical debt (tracked)

- `aria-allowed-attr` on nested multiple-choice markup remains present in integrated assessment and item flows.
- This issue is already known to originate upstream of `pie-players` item host behavior and remains allowlisted in player-scoped baselines until upstream remediation lands.

### Existing deferred hotspots (still active)

- Dialog/floating panel focus contract consistency.
- Keyboard alternatives for hosted shell move/resize interactions.
- Splitpane narrow-width reflow behavior.
- Post-navigation focus ownership policy in section player.

## Manual AT Status

- Keyboard/focus behavior has automated coverage in critical paths.
- Full assistive-technology validation (VoiceOver/NVDA/JAWS) is still required and remains a tracked evidence gap.
- Manual AT execution should follow `evaluation-method.md` and update `deferred-issues.md` statuses after completion.
