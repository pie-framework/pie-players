// Entry point for the pie-fixed-player library build.
// This version is designed to work with pre-bundled PIE elements
// that are included at build time rather than loaded dynamically.
import "./PieFixedPlayer.svelte";
