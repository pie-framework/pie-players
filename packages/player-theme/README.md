# @pie-players/pie-theme

Generic theming wrapper custom element for PIE players.

```html
<pie-theme theme="light" scope="self">
  <!-- PIE player content -->
</pie-theme>
```

Attributes:

- `theme`: `light | dark | auto`
- `scope`: `self | document`
- `variables`: JSON string of `--pie-*` overrides

