/**
 * The type the vendor wrappers check `components.ts` against. Their `rootDir`
 * stops at their own package, so they cannot compile this package's source,
 * and the shells type as `any`.
 */
export declare const CalculatorInlineTool: any;
export declare const CalculatorTool: any;
