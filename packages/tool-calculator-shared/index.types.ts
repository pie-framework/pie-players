/**
 * The published root types entry, declaring what `index.ts` exports: the
 * components the vendor wrappers compile into their own custom elements. They
 * are typed here without Svelte because hosts install none.
 */
export declare const CalculatorInlineTool: any;
export declare const CalculatorTool: any;
